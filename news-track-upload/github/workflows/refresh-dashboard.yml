name: Refresh Dashboard

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */2 * * *"

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          python -m playwright install chromium

      - name: Generate dashboard
        run: |
          python scrape_india_news_trending.py \
            --site all \
            --headless \
            --out_csv web/india_trending.csv \
            --out_json web/india_trending.json \
            --out_html web/index.html

      - name: Commit updated artifacts
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add web/india_trending.csv web/india_trending.json web/index.html
          git diff --cached --quiet || git commit -m "Refresh dashboard data"
          git push

