name: Refresh Trending Dashboard

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */2 * * *"

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r trend-seven-upload/requirements.txt
          python -m playwright install chromium

      - name: Generate dashboard
        run: |
          cd trend-seven-upload
          python scrape_trending_sources.py \
            --site all \
            --headless \
            --out_csv web/trending_sources.csv \
            --out_json web/trending_sources.json \
            --out_html web/index.html

      - name: Commit updated artifacts
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add trend-seven-upload/web/trending_sources.csv trend-seven-upload/web/trending_sources.json trend-seven-upload/web/index.html
          git diff --cached --quiet || git commit -m "Refresh trending dashboard data"
          git push
